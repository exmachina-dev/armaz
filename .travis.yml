language: python
python:
  - "3.3"
  - "3.4"
# command to install dependencies
install:
  - "pip install ."
  - "pip install -r requirements.txt --allow-external"
# command to run tests
script: nosetests
